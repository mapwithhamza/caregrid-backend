# Implementation Rules

- Build small and verify after every step.
- Never silently rename columns.
- Never silently change category values.
- Validate CSV row counts before serving data.
- Fail clearly if required CSV files are missing.
- Keep backend/frontend response schema stable.
- Add tests after implementing endpoints.
- Use real CSV files, not generated mock data.
- Keep functions typed and readable.
