fastapi
uvicorn
pandas
pydantic
pydantic-settings
python-dotenv
pytest
httpx
pytest-asyncio

# Standalone CareGrid Vector Agent (caregrid_vector_agent/) optional integrations
# Both packages are lazy-imported and only contacted when the corresponding
# enable-flag + credentials are set in the environment. The backend boots and
# /agent/recommend works without either of them installed.
databricks-vectorsearch>=0.40
tavily-python>=0.3.0
